namespace = giga_dismantle

# Dyson swarm 1
planet_event = {
    id = giga_dismantle.001
    is_triggered_only = yes
    hide_window = yes

    trigger = {
        has_planet_flag = giga_dismantle_not_cancelled
    }
    immediate = {
        remove_carrier_flag = has_megastructure
        remove_planet_flag = giga_dismantle_not_cancelled
        remove_modifier = dyson_swarm_1_mod
    }
}

# Dyson swarm 2
planet_event = {
    id = giga_dismantle.002
    is_triggered_only = yes
    hide_window = yes

    trigger = {
        has_planet_flag = giga_dismantle_not_cancelled
    }
    immediate = {
        remove_carrier_flag = has_megastructure
        remove_planet_flag = giga_dismantle_not_cancelled
        remove_modifier = dyson_swarm_2_mod
    }
}

# Dyson swarm 3
planet_event = {
    id = giga_dismantle.003
    is_triggered_only = yes
    hide_window = yes

    trigger = {
        has_planet_flag = giga_dismantle_not_cancelled
    }
    immediate = {
        remove_carrier_flag = has_megastructure
        remove_planet_flag = giga_dismantle_not_cancelled
        remove_modifier = dyson_swarm_3_mod
    }
}

# Asteroid Manufactory
planet_event = {
    id = giga_dismantle.004
    is_triggered_only = yes
    hide_window = yes

    trigger = {
        has_planet_flag = giga_dismantle_not_cancelled
    }
    immediate = {
        remove_carrier_flag = has_megastructure
        remove_carrier_flag = megastructure
        remove_planet_flag = giga_dismantle_not_cancelled
    }
}

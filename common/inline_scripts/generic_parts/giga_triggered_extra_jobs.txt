inline_script = {
    script = $file$

    jobs = $jobs$

    add = "$add$"

    type = "$type$"

    condition = "$condition$"

    multiplier = "$multiplier$"
}

inline_script = {
    script = $file$

    jobs = @[ $jobs$ * $extra_mult$ * $district$ ]

    add = "$add$"

    type = "$type$"

    condition = "
        $extra_condition$
        $condition$
    "

    multiplier = "$multiplier$"
}
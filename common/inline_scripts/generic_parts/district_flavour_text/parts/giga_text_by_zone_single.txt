inline_script = {
    script = generic_parts/district_flavour_text/giga_district_flavour_text
    trigger = "
        num_zones = { type = $zone$ value > 0 }
        $condition$
    "
    key = "$key$"
}
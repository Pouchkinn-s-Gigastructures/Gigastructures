inline_script = {
    script = generic_parts/district_flavour_text/giga_district_flavour_text
    trigger = "
        num_zones = { type = $zone1$ value > 0 }
        num_zones = { type = $zone2$ value > 0 }
        $condition$
    "
    key = "$key$"
}
inline_script = {
    script = generic_parts/giga_triggered_extra_jobs
    extra_condition = "
        owner = { giga_empire_has_extra_virtual_jobs = yes }
    "
    extra_mult = $virtual_mult$

    file = "$file$"

    jobs = $jobs$

    add = "$add$"

    type = "$type$"

    condition = "$condition$"

    district = "$district$"

    multiplier = "$multiplier$"
}

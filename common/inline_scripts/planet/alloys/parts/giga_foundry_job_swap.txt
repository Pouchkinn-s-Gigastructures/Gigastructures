# regular empires
inline_script = {
    script = generic_parts/giga_toggled_job
    job = foundry
    add = $add$
    type = $type$
    count = $jobs$
    condition = "
        owner = {
            is_gestalt = no
        }
        NOT = {	has_modifier = penal_colony }
        $condition$
    "
    multiplier = "$multiplier$"
}

# gestalts
inline_script = {
    script = generic_parts/giga_toggled_job
    job = fabricator
    add = $add$
    type = $type$
    count = $jobs$
    condition = "
        owner = {
            is_gestalt = yes
        }
        NOT = {	has_modifier = penal_colony }
        $condition$
    "
    multiplier = "$multiplier$"
}

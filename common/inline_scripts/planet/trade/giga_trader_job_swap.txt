inline_script = {
    script = generic_parts/giga_triggered_extra_jobs

    file = planet/trade/parts/giga_trader_job_swap

    jobs = $jobs$
    extra_mult = 0.5

    extra_condition = "
        owner = { has_active_tradition = tr_mercantile_commercial_enterprise }
    "

    add = "$mode$"
    mode = @job_mode_add

    type = "$type$"
    type = @modifier_mode_planet

    condition = "$condition$"
    condition = ""

    district = $district$
    district = @no

    multiplier = "$multiplier$"
    multiplier = 1
}
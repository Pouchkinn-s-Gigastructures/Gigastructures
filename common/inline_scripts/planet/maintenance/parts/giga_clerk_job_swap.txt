inline_script = {
    script = generic_parts/giga_toggled_job
    job = clerk
    add = $add$
    count = $jobs$
    condition = "$condition$"
    description = $description$
    multiplier_string = "$multiplier_string$"
}
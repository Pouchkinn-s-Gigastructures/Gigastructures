inline_script = {
    script = planet/sentient_metal/parts/giga_cultivator_job_swap

    jobs = $jobs$

    add = "$mode$"
    mode = @job_mode_add

    type = "$type$"
    type = @modifier_mode_planet

    condition = "$condition$"
    condition = ""

    district = $district$
    district = @no

    multiplier = "$multiplier$"
    multiplier = 1
}
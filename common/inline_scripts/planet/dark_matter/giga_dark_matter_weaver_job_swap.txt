inline_script = {
    script = planet/dark_matter/parts/giga_dark_matter_weaver_job_swap

    jobs = $jobs$

    add = "$mode$"
    mode = @job_mode_add

    type = "$type$"
    type = @modifier_mode_planet

    condition = "$condition$"
    condition = ""

    district = $district$
    district = @no

    multiplier = "$multiplier$"
    multiplier = 1
}
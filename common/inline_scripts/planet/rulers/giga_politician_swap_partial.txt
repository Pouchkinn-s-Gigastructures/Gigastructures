# same as swap but no politician desc, for when you're not swapping them all

inline_script = {
    script = planet/parts/politician
    AMOUNT = $AMOUNT$
}

inline_script = {
    script = planet/parts/politician_swaps
    AMOUNT = $AMOUNT$
}
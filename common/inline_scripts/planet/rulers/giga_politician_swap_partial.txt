# same as swap but no politician desc, for when you're not swapping them all

inline_script = {
    script = planet/rulers/parts/politician
    AMOUNT = $AMOUNT$
}

inline_script = {
    script = planet/rulers/parts/politician_swaps
    AMOUNT = $AMOUNT$
}
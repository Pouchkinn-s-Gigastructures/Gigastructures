inline_script = {
    script = planet/rulers/giga_politician_swap_manual

    jobs = $jobs$
    swaps = @[ -1 * ( (-1 * ($jobs$ * 0.5)) - ((((-1 * ($jobs$ * 0.5)) % 1) + 1) % 1)) ] # half rounded up
}
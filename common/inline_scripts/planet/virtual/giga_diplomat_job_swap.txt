inline_script = {
    script = planet/virtual/parts/giga_diplomat_job_swap

    jobs = $jobs$

    add = "$mode$"
    mode = @job_mode_add

    condition = "$condition$"
    condition = ""

    district = $district$
    district = @no

    description = $description$
    description = @yes

    multiplier_string = "$multiplier_string$"
    multiplier_string = ""
}
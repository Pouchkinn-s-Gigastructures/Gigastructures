inline_script = {
    script = generic_parts/giga_virtual_district_extra_jobs

    file = "planet/research/parts/giga_biologist_job_swap"

    jobs = $jobs$
    virtual_mult = 0.5

    add = "$mode$"
    mode = @job_mode_add

    type = "$type$"
    type = @modifier_mode_planet

    condition = "$condition$"
    condition = ""

    district = $district$
    district = @no

    multiplier = "$multiplier$"
    multiplier = 1
}

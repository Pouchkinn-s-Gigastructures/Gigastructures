inline_script = {
    script = planet/research/parts/giga_researcher_job_swap

    jobs = @[ $jobs$ * ( 1 + $district$ * 0.5 ) ]

    add = $add$
    condition = $condition$

    description = $description$
    multiplier_string = $multiplier_string$
}
inline_script = {
    script = buildings/elysium/giga_elysium_shared_capital_building
    level = $level$
    level_plus_one = @[ $level$ + 1 ]

    upgrade_pops = 1500 # half compared to the others because they go straight from level 1 to level 3 so it doubles

    potential = "
        owner? = {
            is_hive_empire = yes
        }
    "
}

######################################################
# Modifiers

planet_modifier = {
    planet_housing_add = @[ 500 * ( $level$ + 1 ) ]
    planet_amenities_add = @[ 400 * ( $level$ + 1 ) ]
    job_coordinator_add = @[ 50 * ( $level$ + 3 ) ]
    job_logistics_drone_add = @[ 100 * ( $level$ + 1 ) ]
    job_patrol_drone_add = @[ 200 * $level$ ]

    planet_defense_armies_add = @[ 4 * $level$ ]
}

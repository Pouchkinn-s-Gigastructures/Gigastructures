#TODO: special type for virtual, change those conditions lmao

# normal, 75 jobs per 100 pops
inline_script = {
    script = buildings/elysium/parts/giga_elysium_host_jobs
    type = $type$
    mode = @job_mode_per_pop
    condition = "always = no"
}

# virtual, jobs per district built
inline_script = {
    script = buildings/elysium/parts/giga_elysium_host_jobs
    type = $type$
    mode = @job_mode_add
    condition = "always = yes"
}
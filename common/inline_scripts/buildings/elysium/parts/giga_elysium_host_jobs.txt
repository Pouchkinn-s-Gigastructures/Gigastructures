
########################################################################################################################
# technicians

inline_script = {
    script = planet/energy/giga_technician_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|technician|"
}

########################################################################################################################
# miners

inline_script = {
    script = planet/minerals/giga_miner_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|miner|"
}

########################################################################################################################
# farmers

inline_script = {
    script = planet/food/giga_farmer_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|farmer|"
}
########################################################################################################################
########################################################################################################################

########################################################################################################################
# artisans

inline_script = {
    script = planet/consumer_goods/giga_artisan_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|artisan|"
}

########################################################################################################################
# metallurgists

inline_script = {
    script = planet/alloys/giga_foundry_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|metallurgist|"
}

########################################################################################################################
# Supertensiles

inline_script = {
    script = planet/supertensiles/giga_megaengineer_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|megaengineer|"
}

########################################################################################################################
# traders

inline_script = {
    script = planet/trade/giga_trader_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|trader|"
}
########################################################################################################################
########################################################################################################################

########################################################################################################################
# physicists

inline_script = {
    script = planet/research/giga_physicist_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|physicist|"
}

########################################################################################################################
# biologist

inline_script = {
    script = planet/research/giga_biologist_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|biologist|"
}

########################################################################################################################
# engineer

inline_script = {
    script = planet/research/giga_engineer_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|engineer|"
}

########################################################################################################################
# unity jobs

inline_script = {
    script = planet/unity/giga_unity_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|bureaucrat|"
}

########################################################################################################################
########################################################################################################################

########################################################################################################################
# soldiers

inline_script = {
    script = planet/soldiers/giga_soldier_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|soldier|"
}

########################################################################################################################
# enforcers

inline_script = {
    script = planet/security/giga_security_job_swap
    jobs = $jobs$
    mode = $mode$
    condition = "$condition$"
    multiplier = "value:giga_elysium_job_mult|VIRTUAL|$virtual$|JOB|enforcer|"
}

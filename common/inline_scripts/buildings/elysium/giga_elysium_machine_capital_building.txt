cost_level = @[ $level$ + 1 ]inline_script = {
    script = buildings/elysium/giga_elysium_shared_capital_building
    level = $level$
    level_plus_one = @[ $level$ + 1 ]

    potential = "
        owner? = {
            is_machine_empire = yes
        }
    "
}

planet_modifier = {
    planet_housing_add = @[ 500 * $level$ ]
    planet_amenities_add = @[ 600 * $level$ ]
    job_patrol_drone_add = @[ 200 * $level$ ]
    job_replicator_add = 200
    planet_defense_armies_add = @[ 4 * $level$ ]
}

######################################################
# Assimilator

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { has_civic = civic_machine_assimilator }
    }
    modifier = {
        job_replicator_add = -100
        job_logistics_drone_add = 100
    }
}

######################################################
# Shroud Forged

triggered_planet_modifier = {
    potential = {
        exists = owner
        owner = { has_modifier = shroud_forged_physician_modifier }
    }
    modifier = {
        job_physician_drone_add = 100
    }
}

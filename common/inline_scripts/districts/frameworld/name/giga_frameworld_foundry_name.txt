# Hive: Fabrication Nexus
triggered_name = {
	trigger = {
		exists = owner
		owner = {
			is_hive_empire = yes
		}
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_hive_1
}
triggered_flavor_desc = {
	trigger = {
		exists = owner
		owner = {
			is_hive_empire = yes
		}
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_hive_1_desc
}

# Machine: Heavy Industry
triggered_name = {
	trigger = {
		exists = owner
		owner = {
			is_machine_empire= yes
		}
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_machine_1
}
triggered_flavor_desc = {
	trigger = {
		exists = owner
		owner = {
			is_machine_empire= yes
		}
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_machine_1_desc
}

# Individual: Urban Foundries
triggered_name = {
	trigger = {
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_1
}
triggered_flavor_desc = {
	trigger = {
		num_zones = {
			type = zone_foundry
			value > 0
		}
	}
	text = city_foundry_1_desc
}
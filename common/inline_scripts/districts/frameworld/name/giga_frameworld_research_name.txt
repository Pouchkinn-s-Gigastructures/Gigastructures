# Hive: Research Clusters
triggered_name = {
	trigger = {
		exists = owner
		owner = {
			is_hive_empire = yes
		}
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
		
	}
	text = district_hive_research_plural
}
triggered_flavor_desc = {
	trigger = {
		exists = owner
		owner = {
			is_hive_empire = yes
		}
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
	}
	text = district_hive_research_desc
}

# Machine: Research Nexuses
triggered_name = {
	trigger = {
		exists = owner
		owner = {
			is_machine_empire = yes
		}
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
	}
	text = district_nexus_research_plural
}
triggered_flavor_desc = {
	trigger = {
		exists = owner
		owner = {
			is_machine_empire = yes
		}
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
	}
	text = district_nexus_research_desc
}

# Individual: Research Arcologies
triggered_name = {
	trigger = {
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
	}
	text = district_arcology_research_plural
}

triggered_flavor_desc = {
	trigger = {
		OR = {
			num_zones = {
				type = zone_research
				value > 0
			}
			has_any_specialized_research_zone = yes
		}
	}
	text = district_arcology_research_desc
}
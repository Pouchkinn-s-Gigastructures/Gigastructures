# Trenches
triggered_name = {
    trigger = {
        OR = {
            num_zones = {
                type = zone_maginot_fortified_trenches
                value > 0
            }
            num_zones = {
                type = zone_maginot_ringworld_fortified_trenches
                value > 0
            }
        }
    }
    text = zone_maginot_fortified_trenches
}

# Training Facilities
triggered_name = {
    trigger = {
        OR = {
            num_zones = {
                type = zone_maginot_specialized_training_facilities
                value > 0
            }
            num_zones = {
                type = zone_maginot_ringworld_specialized_training_facilities
                value > 0
            }
        }
    }
    text = zone_maginot_specialized_training_facilities
}

# Recovery Zones
triggered_name = {
    trigger = {
        OR = {
            num_zones = {
                type = zone_maginot_recovery_zones
                value > 0
            }
            num_zones = {
                type = zone_maginot_ringworld_recovery_zones
                value > 0
            }
        }
    }
    text = zone_maginot_recovery_zones
}

# Killing Fields
triggered_name = {
    trigger = {
        OR = {
            num_zones = {
                type = zone_maginot_killing_fields
                value > 0
            }
            num_zones = {
                type = zone_maginot_ringworld_killing_fields
                value > 0
            }
        }
    }
    text = zone_maginot_killing_fields
}

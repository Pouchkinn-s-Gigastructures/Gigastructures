# Shield Generator Arrays
triggered_name = {
    trigger = {
        OR = {
            num_zones = {
                type = zone_maginot_shield_generators
                value > 0
            }
            num_zones = {
                type = zone_maginot_ringworld_shield_generators
                value > 0
            }
        }
    }
    text = zone_maginot_shield_generators_global_shield_network
}

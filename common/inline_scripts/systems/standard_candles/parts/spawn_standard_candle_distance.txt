inline_script = {
    script = systems/standard_candles/parts/spawn_standard_candle_rotation

    rotation_increment = $rotation_increment$
    distance_increment = $distance_increment$
    min_distance = $min_distance$

    rotation_iterator = $rotation_iterator$
    distance_iterator = $distance_iterator$
}

inline_script = {
    script = generic_parts/giga_toggled_code

    code = "
        inline_script = {
            script = systems/standard_candles/parts/spawn_standard_candle_distance

            rotation_increment = $rotation_increment$
            distance_increment = $distance_increment$
            min_distance = $min_distance$

            rotation_iterator = $rotation_iterator$
            distance_iterator = @[ $distance_iterator$ - 1 ]
        }
    "

    toggle = @[ $distance_iterator$ - 1 ]
}
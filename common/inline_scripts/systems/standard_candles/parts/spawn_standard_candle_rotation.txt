inline_script = {
    script = systems/standard_candles/parts/spawn_standard_candle_system

    rotation = @[ $rotation_iterator$ * $rotation_increment$ ]
    distance = @[ ( $distance_iterator$ * $distance_increment$ ) + $min_distance$ ]
}

inline_script = {
    script = generic_parts/giga_toggled_code

    code = "
        inline_script = {
            script = systems/standard_candles/parts/spawn_standard_candle_rotation

            rotation_increment = $rotation_increment$
            distance_increment = $distance_increment$
            min_distance = $min_distance$

            rotation_iterator = @[ $rotation_iterator$ - 1 ]
            distance_iterator = $distance_iterator$
        }
    "

    toggle = @[ $rotation_iterator$ - 1 ]
}
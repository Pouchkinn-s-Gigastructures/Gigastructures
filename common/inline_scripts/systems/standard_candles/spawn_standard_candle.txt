
random_list = {
    inline_script = {
        script = systems/standard_candles/parts/spawn_standard_candle_distance

        rotation_increment = @[ 360 / $rotation_steps$ ]
        distance_increment = @[ ($max_distance$ - $min_distance$) / $distance_steps$ ]
        min_distance = $min_distance$

        rotation_iterator = $rotation_steps$
        distance_iterator = $distance_steps$
    }
}
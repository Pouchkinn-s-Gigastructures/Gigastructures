inline_script = {
    script = megastructures/dyson_sphere/dyson_sphere_template
    type = swarm
    class = $class$
    prev_stage = 3
    entity = $entity$
    construction_entity = $construction_entity$
    portrait = GFX_megastructure_construction_background
    upgrade_from = ""

    build_time = @[ 3600 * giga_$class$_star_time_mult ]
    cost_alloys = @[ 10 * giga_$class$_star_cost_mult * 1000 ]
    cost_unity = @[ 5 * giga_$class$_star_cost_mult * 1000 ]

    produces = "
        energy = @[ 1 * giga_$class$_star_production_mult * 1000 ]
    "

    upkeep = ""

    potential = "		
        OR = {
            has_machine_age_dlc = no
            NOT = { has_global_flag = vanilla_dyson_swarm_replace_disabled }
        }
        OR = {
			has_global_flag = vanilla_dyson_capped_u
			check_variable = {
				which = giga_current_vanilla_dyson
				value < value:giga_vanilla_dyson_limit
			}
		}
    "
    possible = ""

    on_build_start = "from = { change_variable = { which = giga_current_vanilla_dyson value = 1 } }"

    on_build_cancel = "from = { change_variable = { which = giga_current_vanilla_dyson value = -1 } }"

    on_complete = "
        fromfrom = {
            upgrade_megastructure_to = dyson_sphere_2_$class$_star
            finish_upgrade = yes
        }
        from = {
            if = {
                limit = {
                    check_variable = {
                        which = dyson_swarm_counter
                        value >= 1
                    }
                }
                change_variable = {
                    which = dyson_swarm_counter
                    value = -1
                }
            }
        }
    "
}

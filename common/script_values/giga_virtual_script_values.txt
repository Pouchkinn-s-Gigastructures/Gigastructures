# virtual colony size

# modifiers to virtual country values
# based on the variable calculated in events since we can't gather in a trigger
giga_virtuality_concentrated_production_mult = {
    weight = owner.value:giga_var|var|giga_virtual_colony_size|mult|0.1|
}
giga_virtuality_num_colonies = {
    weight = owner.value:giga_var|var|giga_virtual_colony_size|mult|0.1|
}

# per planet extra size
# calculated to exclude the base 1
giga_virtuality_colony_size_delta = {
    base = 1 # add base for calc
    add = modifier:giga_virtual_colony_size

    divide = value:scripted_modifier_mult|MODIFIER|giga_virtual_colony_efficiency|

    subtract = 1 # remove base
}

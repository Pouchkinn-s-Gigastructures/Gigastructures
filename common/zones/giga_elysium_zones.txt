# host world replacement government zone
zone_giga_elysium_host = {
    icon = GFX_diplomatic_weight_large
    base_buildtime = @zone_buildtime
    potential = { # planet scope
    }

    unlock = { # planet scope
        has_modifier = giga_elysium_host_planet
    }

    resources = {
        category = planet_zones
    }

    max_buildings = 3

    zone_sets = {
        default
    }

    included_building_sets = { giga_elysium_host }

    convert_to = { zone_default zone_default_giga }

    ai_priority = 0
}

# urban resource zones
@urban_zone_jobs = 100

zone_giga_elysium_host_energy = {
    inline_script = zones/giga_elysium_host_urban_zone

    icon = GFX_district_specialization_energy

    included_building_sets = {
        urban
        generator
        fallen_empire_generator
    }

    inline_script = {
        script = planet/energy/giga_technician_job_swap
        jobs = @[ urban_zone_jobs * 0.5 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/consumer_goods/giga_artisan_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        condition = "
            owner = { country_uses_consumer_goods = yes }
        "

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/trade/giga_trader_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        condition = "
            owner = { country_uses_consumer_goods = no }
        "

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/trade/giga_trader_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
}
zone_giga_elysium_host_minerals = {
    inline_script = zones/giga_elysium_host_urban_zone

    icon = GFX_district_specialization_minerals

    included_building_sets = {
        urban
        mining
        fallen_empire_mining
    }

    inline_script = {
        script = planet/minerals/giga_miner_job_swap
        jobs = @[ urban_zone_jobs * 0.5 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/alloys/giga_foundry_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/trade/giga_trader_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
}
zone_giga_elysium_host_food = {
    inline_script = zones/giga_elysium_host_urban_zone

    icon = GFX_district_specialization_food

    included_building_sets = {
        urban
        farming
        hydroponics
        fallen_empire_farming
    }

    inline_script = {
        script = planet/food/giga_farmer_job_swap
        jobs = @[ urban_zone_jobs * 0.5 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/unity/giga_unity_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
    inline_script = {
        script = planet/trade/giga_trader_job_swap
        jobs = @[ urban_zone_jobs * 0.25 ]
        district = @yes
        type = @modifier_mode_district_planet

        multiplier = "value:giga_jobs_per_district_mult"
    }
}